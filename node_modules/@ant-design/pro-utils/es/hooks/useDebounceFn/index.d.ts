import type { DependencyList } from 'react';
export declare type ReturnValue<T extends any[], U = any> = {
    run: (...args: T) => Promise<U>;
    cancel: () => void;
};
declare function useDebounceFn<T extends any[], U = any>(fn: (...args: T) => Promise<any>, deps: DependencyList | number, wait?: number): ReturnValue<T, U>;
export default useDebounceFn;
